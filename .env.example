# StreetFlood API Environment Variables
# API endpoints
FRONTEND_URL=http://localhost:3000
BACKEND_PORT=8000
OSRM_BACKEND_URL=http://localhost:5000

# Open-Meteo requires no API key, but if we add a paid API later, define it here
# WEATHER_API_KEY=your_api_key_here
